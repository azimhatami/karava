# کارآوا (Karava) — معرفی جامع پروژه

مستند وضعیت فعلی سامانه برای ارائه دانشگاهی. تمرکز روی معماری، قابلیت‌ها و جریان‌های کاری است، نه جزئیات پیاده‌سازی سطح فایل.

---

## ۱. معرفی کلی پروژه

**کارآوا** یک بازار آنلاین فریلنسری با رابط کاربری فارسی و راست‌به‌چپ (RTL) است که کارفرمایان و فریلنسرها را برای تعریف پروژه، ارسال پیشنهاد، همکاری و تسویه حساب به هم متصل می‌کند.

### مسئله‌ای که حل می‌کند

در بازار کار پروژه‌ای، معمولاً این چالش‌ها وجود دارد:

- کارفرما نمی‌تواند به‌سادگی پروژه را اعلام کند و پیشنهادهای متعدد را مقایسه کند.
- فریلنسر کانال شفافی برای پیدا کردن فرصت و ارائه قیمت/زمان ندارد.
- اعتماد بین طرفین (تأیید هویت، نگه‌داری بودجه تا پایان کار، امتیازدهی) ضعیف است.

کارآوا این چرخه را در یک پلتفرم یکپارچه پوشش می‌دهد: از مشاهده پروژه تا پذیرش پیشنهاد، گفتگو، تکمیل، پرداخت آزمایشی (کیف پول داخلی) و ثبت نظر.

### نقش‌های کاربری

| نقش | توضیح |
|-----|--------|
| **مهمان** | بدون ورود می‌تواند پروژه‌ها و پروفایل عمومی کاربران را ببیند. |
| **کارجو / فریلنسر (`FREELANCER`)** | جستجوی پروژه، ارسال پیشنهاد، گفتگو، تحویل کار، کیف پول و نظر. |
| **کارفرما (`OWNER`)** | ثبت و مدیریت پروژه، بررسی پیشنهادها، پذیرش/رد، گفتگو، تکمیل و پرداخت. |
| **مدیر سیستم (`ADMIN`)** | تأیید یا رد کاربران و نظارت بر پروژه‌ها و پیشنهادها. |

کاربر پس از ورود با OTP، نقش خود را انتخاب می‌کند و تا زمان تأیید ادمین، وضعیتش «در انتظار» می‌ماند؛ پس از تأیید، به پنل اختصاصی دسترسی پیدا می‌کند.

---

## ۲. معماری فنی

### استک تکنولوژی

| لایه | تکنولوژی |
|------|-----------|
| **فرانت‌اند** | React 18، Vite، React Router، Tailwind CSS، TanStack React Query، Axios، React Hook Form، Headless UI |
| **بک‌اند** | Node.js، Express |
| **دیتابیس** | MongoDB با Mongoose |
| **احراز هویت** | OTP پیامکی + JWT در کوکی‌های HTTP-only |
| **اعتبارسنجی** | Joi (با پشتیبانی از ارقام فارسی/عربی) |
| **پیامک** | سرویس کاوه‌نگار (در production) |
| **آپلود فایل** | Multer + ذخیره محلی روی دیسک سرور |
| **تاریخ** | پشتیبانی از تقویم جلالی در رابط کاربری |

ارتباط کلاینت و سرور از نوع **REST API** است (`/api/...`). فرانت‌اند درخواست‌ها را از طریق پروکسی توسعه یا آدرس API به بک‌اند می‌فرستد.

### الگوی معماری

معماری کلی به‌صورت **کلاینت–سرور جدا** است:

- **فرانت‌اند:** لایه‌بندی بر اساس صفحات (`pages`)، دامنه‌های کسب‌وکار (`features` مثل auth، owner، freelancer، admin، wallet، messages، review)، سرویس‌های HTTP و کامپوننت‌های مشترک UI.
- **بک‌اند:** مسیرها (Router) → کنترلرها → مدل‌های Mongoose، به‌همراه middlewareهای احراز هویت، نقش و تأیید کاربر، و اعتبارسنجی با Joi.

```
[مرورگر / React]
        │  REST + Cookie (JWT)
        ▼
[Express API]
        │
        ▼
[MongoDB]
```

### احراز هویت (OTP + JWT)

1. کاربر نقش ورود را انتخاب می‌کند و شماره موبایل ایرانی (`09...`) را وارد می‌کند.
2. سرور یک کد OTP شش‌رقمی تولید می‌کند (در محیط توسعه ثابت `111111`؛ در production از کاوه‌نگار ارسال می‌شود).
3. پس از تأیید OTP، دو کوکی امضاشده تنظیم می‌شود: **Access Token** (کوتاه‌مدت) و **Refresh Token** (بلندمدت).
4. اگر پروفایل کامل نباشد، کاربر به مرحله تکمیل پروفایل (نام، ایمیل، نقش) هدایت می‌شود.
5. تا وقتی ادمین وضعیت کاربر را «تأییدشده» نکند، دسترسی به عملیات حساس پنل (پروژه، پیشنهاد، کیف پول، چت و …) مسدود است.
6. مسیرهای محافظت‌شده در فرانت‌اند با گارد نقش و در بک‌اند با بررسی توکن + نقش + وضعیت تأیید کنترل می‌شوند.

---

## ۳. قابلیت‌های پیاده‌سازی‌شده (به‌تفکیک نقش)

### ۳.۱ عمومی / مهمان

- صفحه اصلی با لیست پروژه‌های باز
- جستجو و فیلتر بر اساس عنوان/توضیح/تگ، دسته‌بندی و وضعیت (باز/بسته)
- صفحه جزئیات عمومی پروژه
- پروفایل عمومی کاربر به‌همراه امتیاز و نظرات
- مشاهده دسته‌بندی‌ها
- ورود / ثبت‌نام با OTP و تکمیل پروفایل
- رابط کاربری فارسی RTL و پشتیبانی از تم روشن/تاریک

### ۳.۲ فریلنسر (کارجو)

- داشبورد اختصاصی با آمار خلاصه
- مرور پروژه‌های باز و ارسال پیشنهاد (قیمت، مدت، واحد روز/هفته/ماه، توضیحات)
- مشاهده لیست پیشنهادهای خود
- گفتگوی درون‌پنلی پس از پذیرش پیشنهاد
- کیف پول آزمایشی (موجودی، شارژ، تاریخچه تراکنش)
- ویرایش پروفایل، مهارت‌ها و آپلود نمونه‌کار (پورتفolio)
- آپلود فایل‌های تحویل (deliverables) برای پروژهٔ پذیرفته‌شده
- ثبت نظر و امتیاز برای کارفرما پس از تکمیل پروژه

### ۳.۳ کارفرما

- داشبورد اختصاصی با آمار پروژه‌ها
- ایجاد، ویرایش و مدیریت پروژه‌ها (عنوان، توضیح، بودجه، مهلت، تگ، دسته‌بندی، پیوست)
- تغییر وضعیت پروژه (باز / بسته) و اعلام تکمیل
- مشاهده پیشنهادها و پذیرش یا رد آن‌ها (با منطق امانت کیف پول)
- گفتگو با فریلنسر پذیرفته‌شده
- کیف پول آزمایشی و مشاهده تراکنش‌ها
- ویرایش پروفایل و اطلاعات شرکت
- ثبت نظر و امتیاز برای فریلنسر پس از تکمیل پروژه

### ۳.۴ مدیر سیستم

- داشبورد ادمین
- لیست کاربران و تغییر وضعیت تأیید (رد / در انتظار / تأیید)
- مشاهده پروژه‌ها و پیشنهادها برای نظارت
- مدیریت پروفایل خود
- API مدیریت دسته‌بندی‌ها در بک‌اند موجود است؛ رابط گرافیکی کامل مدیریت دسته در پنل ادمین محدود/ناقص نسبت به API است

---

## ۴. مدل‌های دیتابیس

سامانه روی MongoDB با مدل‌های Mongoose زیر بنا شده است.

### ۴.۱ خلاصه مدل‌ها و فیلدهای کلیدی

#### User (کاربر)
- اطلاعات هویتی: نام، ایمیل، موبایل، آواتار، بیوگرافی
- نقش: `OWNER` | `FREELANCER` | `ADMIN`
- وضعیت تأیید ادمین: `0` ردشده، `1` در انتظار، `2` تأییدشده
- فیلدهای پروفایل: مهارت‌ها، نام/توضیح شرکت، نمونه‌کارها
- OTP موقت، پرچم تأیید شماره و فعال‌بودن حساب

#### Project (پروژه)
- عنوان، توضیح، بودجه، تگ‌ها، مهلت
- وضعیت: `OPEN` | `CLOSED` | `COMPLETED`
- ارتباط با دسته‌بندی، کارفرما و (پس از پذیرش) فریلنسر
- لیست پیشنهادها، پیوست‌ها و فایل‌های تحویل
- فیلدهای امانت (Escrow): مبلغ، پیشنهاد مرتبط، وضعیت `none` / `held` / `released` / `refunded`

#### Proposal (پیشنهاد)
- قیمت، مدت، واحد زمان (`day` / `week` / `month`)، توضیح
- ارجاع به کاربر (فریلنسر)
- وضعیت: `0` رد، `1` در انتظار، `2` پذیرفته

#### Category (دسته‌بندی)
- عنوان فارسی، عنوان انگلیسی، توضیح
- امکان ساختار سلسله‌مراتبی (والد)

#### Wallet (کیف پول)
- یک کیف پول یکتا به ازای هر کاربر
- `balance` (موجودی آزاد) و `heldBalance` (مبلغ بلوکه‌شده)

#### WalletTransaction (تراکنش کیف پول)
- نوع: `deposit` | `hold` | `release` | `refund`
- مبلغ، توضیح، ارجاع اختیاری به پروژه/پیشنهاد

#### Review (نظر / امتیاز)
- امتیاز ۱ تا ۵ و متن نظر
- ارجاع به پروژه، پیشنهاد، نویسنده و گیرندۀ نظر
- محدودیت: حداکثر یک نظر از هر کاربر برای هر پروژه

#### Conversation (گفتگو)
- به ازای هر پیشنهاد پذیرفته‌شده یک گفتگو
- ارجاع به پروژه، پیشنهاد، کارفرما و فریلنسر

#### Message (پیام)
- متن پیام، فرستنده، وضعیت خوانده‌شدن
- وابسته به یک Conversation

#### UploadedFile (زیرسند فایل)
- نام فایل، نوع MIME، اندازه، مسیر/کلید ذخیره و URL — به‌صورت توکار در پورتفolio، پیوست پروژه و تحویل‌ها استفاده می‌شود.

### ۴.۲ نمودار سادهٔ روابط (ER متنی)

```
User ──< Project (owner)
User ──< Project (freelancer, اختیاری)
Category ──< Project
Project ──< Proposal (از طریق آرایهٔ proposals)
User ──< Proposal
User ── 1:1 Wallet
Wallet ──< WalletTransaction
Project ── 1:1 Conversation (پس از پذیرش)
Proposal ── 1:1 Conversation
Conversation ──< Message
User ──< Message (sender)
Project ──< Review
User ──< Review (reviewer / reviewee)
```

---

## ۵. جریان‌های کاری اصلی

### ۵.۱ ثبت‌نام و ورود

1. کاربر به صفحه ورود می‌رود و نقش موردنظر (کارجو / کارفرما / مدیر) را انتخاب می‌کند.
2. شماره موبایل را وارد می‌کند و درخواست OTP می‌دهد.
3. کد را وارد می‌کند؛ در صورت صحت، نشست با JWT برقرار می‌شود.
4. اگر حساب تازه باشد یا پروفایل ناقص باشد، نام، ایمیل و نقش را تکمیل می‌کند.
5. وضعیت اولیه معمولاً «در انتظار تأیید ادمین» است.
6. پس از تأیید ادمین، کاربر به داشبورد نقش خود هدایت می‌شود و به قابلیت‌های اصلی دسترسی پیدا می‌کند.

> در محیط توسعه، OTP ثابت `111111` است و ارسال واقعی پیامک لازم نیست.

### ۵.۲ چرخهٔ عمر پروژه (از ثبت تا نظر)

1. **ثبت پروژه:** کارفرما پروژه را با بودجه، مهلت، دسته و جزئیات ایجاد می‌کند (نیاز به پروفایل کامل و حساب تأییدشده).
2. **دریافت پیشنهاد:** فریلنسرها پروژهٔ باز را می‌بینند و پیشنهاد قیمت/زمان می‌فرستند.
3. **پذیرش پیشنهاد:** کارفرما یک پیشنهاد را می‌پذیرد؛ مبلغ توافق‌شده از موجودی کارفرما به حالت بلوکه (Hold) می‌رود و گفتگو بین دو طرف ساخته می‌شود. سایر پیشنهادها معمولاً رد می‌شوند.
4. **گفتگو و همکاری:** طرفین از چت درون‌سامانه‌ای هماهنگ می‌کنند؛ فریلنسر می‌تواند فایل تحویل آپلود کند.
5. **تکمیل پروژه:** کارفرما پروژه را تکمیل‌شده اعلام می‌کند؛ مبلغ بلوکه‌شده به موجودی فریلنسر آزاد می‌شود (Release).
6. **نظر و امتیاز:** هر دو طرف می‌توانند پس از تکمیل، برای یکدیگر امتیاز و نظر ثبت کنند.

### ۵.۳ ارسال پیشنهاد توسط فریلنسر

1. فریلنسر وارد پنل یا صفحه پروژه می‌شود و پروژه‌های باز را می‌بیند.
2. در صورت کامل‌بودن پروفایل و تأیید ادمین، فرم پیشنهاد را پر می‌کند (قیمت، مدت، واحد زمان، توضیح).
3. پیشنهاد با وضعیت «در انتظار» ثبت می‌شود.
4. کارفرما آن را می‌پذیرد یا رد می‌کند؛ در صورت پذیرش، مسیر گفتگو و امانت بودجه فعال می‌شود.

---

## ۶. نکات فنی برجسته

### کیف پول آزمایشی و منطق Escrow
به‌جای درگاه پرداخت واقعی، یک کیف پول داخلی شبیه‌سازی شده است. با پذیرش پیشنهاد، بودجه از موجودی آزاد کارفرما به `heldBalance` منتقل می‌شود و فقط با تکمیل پروژه به فریلنسر آزاد می‌گردد. در صورت لغو پذیرش، مبلغ قابل بازگردانی (Refund) است. این الگو اعتماد حداقلی بین طرفین را بدون اتصال بانکی نشان می‌دهد.

### ارقام فارسی و عربی
کاربران ممکن است اعداد را با کیبورد فارسی یا عربی وارد کنند. هم در فرانت‌اند (نرمال‌سازی ورودی و نمایش با جداکننده هزارگان فارسی) و هم در بک‌اند (تبدیل قبل از اعتبارسنجی Joi) این موضوع پوشش داده شده تا شماره موبایل، OTP و مبالغ دچار خطای اعتبارسنجی نشوند.

### گفتگو با Polling
پس از پذیرش پیشنهاد، Conversation ساخته می‌شود. به‌جای WebSocket، فرانت‌اند با React Query در بازه‌های منظم (چند ثانیه) پیام‌ها و تعداد خوانده‌نشده را از API می‌گیرد. برای مقیاس فعلی پروژه ساده و قابل‌اعتماد است.

### OTP توسعه برای تست و ارائه
در `development` کد OTP ثابت است و SMS ارسال نمی‌شود؛ به‌همراه اسکریپت Seed دیتابیس، راه‌اندازی دمو و تست سناریوهای نقش‌ها سریع انجام می‌شود.

### کنترل دسترسی چندلایه
علاوه بر نقش، «وضعیت تأیید ادمین» و «کامل‌بودن پروفایل» برای عملیات حساس (ساخت پروژه / ارسال پیشنهاد) الزامی است تا کیفیت داده‌ها و اعتماد پلتفرم حفظ شود.

### رابط کاربری ریسپانسیو پنل‌ها
پنل‌های کارفرما، فریلنسر و ادمین برای موبایل با منوی کشویی و نمایش جدولی به‌صورت کارت سازگار شده‌اند تا تجربه استفاده روی صفحات کوچک حفظ شود.

---

## ۷. محدودیت‌های فعلی و کارهای آینده

| موضوع | وضعیت فعلی | جهت آینده |
|--------|-------------|-----------|
| **پیامک واقعی** | فقط در production با کاوه‌نگار؛ در توسعه OTP ثابت | اتصال پایدار کلید API و قالب پیامک برای محیط واقعی |
| **درگاه پرداخت** | شارژ کیف پول کاملاً آزمایشی است | اتصال به درگاه بانکی واقعی |
| **کارمزد پلتفرم** | کل مبلغ پیشنهاد به فریلنسر منتقل می‌شود | تعریف درصد کارمزد و کسر خودکار |
| **چت لحظه‌ای** | Polling دوره‌ای | ارتقا به WebSocket / Server-Sent Events در صورت نیاز مقیاس |
| **ذخیره فایل** | دیسک محلی سرور | فضای ابری (مثلاً S3 یا آروان) |
| **مدیریت دسته در UI ادمین** | API موجود؛ UI کامل محدود | فرم CRUD دسته در پنل ادمین |
| **Docker / استقرار** | راه‌اندازی دستی Node + Mongo | Compose یا پایپ‌لاین استقرار |
| **احراز هویت با رمز عبور** | فیلد در مدل هست ولی استفاده نمی‌شود | در صورت نیاز محصولی قابل فعال‌سازی |
| **داوری اختلاف / بازگشت وجه پیشرفته** | Refund ساده هنگام لغو پذیرش | گردش‌کار حل اختلاف و پشتیبانی |

---

## جمع‌بندی کوتاه برای ارائه

کارآوا یک سامانهٔ بازار فریلنسری فارسی است که با معماری کلاسیک **React + Express + MongoDB**، احراز هویت **OTP و JWT**، نقش‌های کارفرما/فریلنسر/ادمین، چرخهٔ کامل پروژه تا نظر، کیف پول امانی آزمایشی و گفتگوی پس از پذیرش، هستهٔ یک پلتفرم کاریابی پروژه‌ای را پیاده‌سازی کرده است. بخش‌های پرداخت واقعی، کارمزد، پیامک production و زیرساخت ابری به‌عنوان گام‌های بعدی برای آماده‌سازی محیط عملیاتی مطرح‌اند.
